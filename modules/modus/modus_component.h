#pragma once

#include "controller/window_info.h"
#include "resources/common_resources.h"

inline static const WindowInfo kModusWindowInfo = {
    ID_MODUS_VIEW, "Modus", u"Modus", 0, 0, 0, IDR_MODUS_POPUP};
