# SCADA Client — UX Implementation Backlog

> Status: living plan. A **catalogue of the reshell work** derived from
> [`shell.md`](shell.md), grouped for reference — **not a fixed waterfall.**
> Each item names the primary code it touches, its dependency, and a "done when"
> acceptance line.

## How to use this (read first)

Per `client/CLAUDE.md` → "UX implementation approach": build the reshell as
**incremental vertical slices**, not by executing P0→P5 top to bottom. Pick one
coherent surface, take it end-to-end (behind the opt-in theming flag), validate
it against real Qt widgets, ship, then re-evaluate. The groups below are a map
of *what* exists to do and how items depend on each other; the *order* is chosen
per slice with the user, not dictated by the numbering. Theming is **opt-in and
palette-first** — never made unconditional here.

The groups (P0 foundations, P1 shell chrome, P2 operator core, P3 dialog
theming, P4 Explorer/Inspector, P5 engineering) capture dependencies, not a
required sequence.

---

## P0 — Foundations (design system in code)

| # | Item | Touches | Done when |
|---|---|---|---|
| 0.1 | **Token layer** — ✅ *landed (opt-in)*: `aui/qt/theme_qt.{h,cpp}` (`scada::aui`) encodes the light/dark/high-contrast token tables, builds a Fusion `QPalette`, and generates the QSS; `ApplyTheme(theme, scope)` installs palette (+stylesheet for `kFull`). Wired in `app/qt/main.cpp` **behind the `Ux/Experimental` QSetting (off by default)**; `Ux/Theme` picks the variant, `Ux/StyleSheet=false` → palette-only. *Remaining:* drive the choice from `Profile` + a runtime switcher; validate `kFull` against ActiveX/custom-painted widgets before defaulting on. | `app/qt/`, `profile/`, `aui/qt/theme_qt.*` | Opt-in themes switch at runtime; no component hard-codes hex; values match [`design-language.md`](design-language.md). |
| 0.2 | **Severity palette single-source** — ✅ *landed (opt-in)*: `aui/severity_colors.{h,cpp}` (`scada::aui::EventRowColorsFor`) is the one theme-aware map for event/alarm colours; the event journal now resolves its row colours there instead of hardcoded RGB, so a token change restyles it (and future severity surfaces) at once. `SetSeverityTheme` follows the palette opt-in in `app/qt/main.cpp` (`kLegacy` = unchanged default). Unit-tested + validated on the real journal via `--theme=dark`. *Remaining:* wire the tree/inspector/status severity cues as those surfaces gain colouring. | `aui/`, `modules/events/` | Changing a severity token restyles every surface at once. |
| 0.3 | **Mono numerals** — ✅ *landed (opt-in)*: `scada::aui::MonoValueFont()` (`aui/qt/theme_qt.{h,cpp}`) is the one source of the design-language `--font-mono` stack (Cascadia Mono → Consolas → SF Mono/Menlo/DejaVu standing in for `ui-monospace`), sized like the application font and **empty under the legacy theme** so the default look is unchanged. `TableColumn` gained a `monospace` flag and `TreeModel` an `IsMonospaceColumn` hook; the Qt table/tree adapters answer `Qt::FontRole` from them, with `DataType::DateTime` columns monospace implicitly — so the Table value column, the timed-data value column, the journal Value column, every DateTime column, and the object tree's live Value column all render tabular from one seam. `Table` widens a mono column's *default* width by the measured UI→mono advance ratio (`DefaultColumnWidth`) so timestamps that fit the legacy font don't elide; user-saved widths still win. Unit-tested (`MonoValueFontIsTokenThemeGated`, table/tree adapter FontRole gating, column-width widening — 7 cases) and validated on the real Qt table (`--theme=dark` `table.png`: values + full `dd.mm.yyyy hh:mm:ss.zzz` timestamps in mono; legacy capture unchanged). *Remaining:* the generic admin `aui::Grid` (`GridModel` has no column typing) and the parameter-form limits grids don't take the mono font yet; inspector/status readouts adopt it as those surfaces are built. | `aui/models/`, `aui/qt/`, table/tree delegates | Values/timestamps render tabular; columns don't jitter on update. |
| 0.4 | **Themed control primitives**: `Field`, `Combo`, `Checkbox`, `Button` (primary/default/danger/disabled+reason). | new `aui/qt/controls` | Primitives available; retire native `QInputDialog`/`QMessageBox` defaults. |

## P1 — Shell chrome

| # | Item | Touches | Dep | Done when |
|---|---|---|---|---|
| 1.1 | **Activity bar** — ✅ *landed (opt-in)*: a full-height charcoal left rail (`main_window/activity_bar_qt.{h,cpp}`, gated on the active UX theme) with the display-hierarchy sections — Overview, Alarms, Trends, Substations, Tables on top; Administration, Settings pinned at the bottom. Each live section opens its view via `FindWindowInfoByName` → `OpenView` (Alarms→EventJournal, Trends→Graph, Substations→Modus, Tables→Table); sections without a registered view (Overview/Administration/Settings today) are shown disabled. The active section carries an accent marker; the Alarms section carries an unacknowledged-count badge fed by a new `StatusBarModel::GetAlarmCount()` (wired from `EventStatusProvider`, the same source the status strip uses). Section icons reuse each view command's image where present, falling back to a letter glyph. Unit-tested (`activity_bar_unittest.cpp`: click-activates, disabled-gating, active-section, badge; plus the count accessor). Validated on the real window (`--theme=dark`). *Remaining:* back sections with `GlobalCommandRegistry` commands (so they also appear in the palette); dedicated rail icons; wire Overview/Administration/Settings once those pages exist. | new `main_window/qt/activity_bar`, `core/GlobalCommandRegistry` | 0.1 | Sections switch; badge tracks unacknowledged count. |
| 1.2 | **Top context bar** — ✅ *landed (opt-in)*: a full-width bar above the command toolbar with a brand lockup, a command/search field (Russian placeholder via `.ts`), and a live context cluster mirroring the status-bar model panes. Built in `main_window_qt.cpp::CreateContextBar`, gated on the active UX theme (so both the app and the headless generator show it). Validated on the real main window (`--theme=dark`). *Remaining:* wire the field to the command palette (1.3), and curate the cluster to plant/server/connection/user (currently mirrors all status panes). | `main_window/`, `core/` | 0.1 | Context cluster visible; field opens palette (1.3). |
| 1.3 | **Command palette** — ✅ *done (commands + views + type-to-search)*: `Ctrl K`, clicking the command-search field, **or typing a printable key in it** opens a modal palette (`main_window/command_palette_qt.{h,cpp}`) seeded with that character; it filters every registered command by title — case- and Cyrillic-fold-insensitive matching, prefix-before-interior ranking, in the Qt-free `command_match.{h,cpp}` (unit-tested) — and runs the selection through the existing handler resolution. **Views are covered**: the `Open …` view actions (Graph/Display/Table/…) are registered `CATEGORY_OPEN` commands, so they appear in the palette and their global handler opens the view. Validated on the real widget (generator capture) + unit-tested (incl. preset-filter). Address-space **tags** are split out to 1.3a. | `core/`, `controller/` | 1.2 | Fuzzy search finds commands + views; type-to-search; Enter activates. |
| 1.3a | **Palette address-space tags** — ✅ *landed (opt-in)*: data-item tags appear in the command palette and open in a table view when chosen. `NodeService` is now threaded into `MainWindowContext` (optional `NodeService*`, null in minimal/test contexts). `main_window/tag_search_index.{h,cpp}` does a **bounded, cycle-safe background browse** (from `ObjectsFolder` over `Organizes`, collecting `DataItemType` leaves, capped at 2000) kicked off at main-window construction; the palette reads the cached tags and filters them locally via the same matcher as commands, through a new `CommandPalette::ExtraItem` channel (matched/ranked like commands, `activate` runs on Enter). Unit-tested (palette extra-item match + activation). *Remaining/known limits:* the browse is a one-time cache (no incremental refresh, capped — logs nothing when capped yet); activation opens a table (no reveal-in-Explorer); interactive end-to-end validation needs a logged-in server session. | `main_window/`, `common/node_service/` | 1.3 | Typing finds tags by name; Enter opens the node. |
| 1.4 | **Editor-style workspace tabs** — ✅ *landed (opt-in)*: the central workspace `DockTabWidget` (already `documentMode`) is restyled through the theme QSS (`aui/qt/theme_qt.cpp`) to read as editor tabs — flat inactive tabs on the elevated bar, the active tab dropping its separators to blend into the content pane with an accent top marker, hover state, and a hover-highlighted close button; dock panels get a slim themed title strip in place of the native OS title chrome. No `view_manager_qt` submodule edit — pure token-driven styling, so it only applies under the reshell theme. `OpenedView` labels intact; close/reorder unchanged. Validated on the real main window (`--theme=dark` capture). *Remaining:* a custom close glyph (the native red × still shows) and tab-overflow affordance. | `aui/qt/`, `ViewManager` | 0.1 | Tabs restyled; `OpenedView` labels intact; close/reorder works. |
| 1.5 | **Status strip** — ✅ *done*: highest-severity alarm cell (coloured from the severity single source), plus the **user·role** cell (the user pane now shows `<user> · <role>`, role derived from session privileges via the pure `UserRoleKey` — Configure⇒Administrator, Control⇒Operator, else Observer) and an **endpoint/build** cell (`SessionStatusProvider::GetEndpointText` → `<host> · v<version>`, version from the module's `CLIENT_BUILD_VERSION` compile def = `${PROJECT_VERSION}`). Unit-tested (`SeverityColorsTest`, `UserRoleKeyTest`); validated on the real strip (`--theme=dark`): "Администратор", "local · v2.6.0". Russian role labels in `client_ru.ts`. | `main_window/status_bar/`, `aui/` | 0.2 | Alarm summary + user·role + endpoint/build live. |

## P2 — Operator core (the cockpit)

| # | Item | Touches | Dep | Done when |
|---|---|---|---|---|
| 2.1 | **Overview page** — 🔶 *landed (opt-in), one region ambient*: `main_window/overview_page.{h,cpp}` builds the operator landing cockpit in code (Qt-free, so it opens without a saved profile and its structure is unit-testable) — a **dominant trend** (`Graph`) and the **active-alarm table** (`EventJournal` with `mode=Current`, which surfaces only unacknowledged/actionable alarms, per the 2.2 model split). A fresh, page-less profile lands on it: `CreateInitialPage()` returns `MakeOverviewPage()` under the reshell theme and the legacy multi-window default when the reshell is off. The activity rail's Overview section routes to it (`kOverviewSectionId` = `@overview`, `OpenPage`, not `OpenView`). Unit-tested (`OverviewPageTest.HasTrendAndActiveAlarmWindows` + `InitialPageTest.ReshellSeedsTheOverviewPage`). *Remaining:* the **KPI severity strip is not a region of the page** — it is ambient in the top context bar (2.3's `SeverityTileStrip`), so "all three regions" holds only if the context bar counts as one; a page-hosted strip is still open. The page also takes the default layout (no explicit `page.layout` split, so the trend/alarm proportions are not the mockup's), and there is **no headless capture** of it — `client-window.png` builds its own profile, so the Overview landing is covered by the structure tests only. | `profile/`, `modules/graph/`, `modules/events/` | 1.1–1.5 | Fresh profile lands on Overview; all three regions live. |
| 2.2 | **Active-alarm surface vs. event journal split** (principle §3) — 🔶 *in progress*. Discovery: the split already exists at the model layer — `CurrentEventModel::events()` returns `unacked_events()`, so the docked "Event" panel + EventJournal "Current" mode already show only actionable (unacknowledged) alarms, while historical mode is the full journal; severity filter + period (time range) + `ID_ACKNOWLEDGE_ALL` all present. Added the mockup's **"Unacknowledged only"** control at the model layer: `EventTableModel::SetUnacknowledgedOnly` filters the *historical* journal to actionable events via `IsEventShown` (unit-tested — `EventTableModelUnacknowledgedFilterTest`, exercised synchronously through `HistoricalEventModel::refilter_now`). **Journal filter bar landed** (`modules/events/qt/event_filter_bar.{h,cpp}`): a cross-platform (Qt) filter strip on the full journal — an **"Unacknowledged only"** toggle (→ `SetUnacknowledgedOnly`) and a **Min. severity** spinbox (0 = "All", → `SetSeverityMin`) — wrapping the table in `EventView::Init` under a `#if defined(UI_QT)` block, opt-in on the reshell theme, journal-only (not the docked panel). Russian in `client_ru.ts`; validated on the real journal (`--theme=dark`). **Area + Period controls landed** (completing the filter set): an **Area** dropdown that browses the top-level areas (via `BrowseEventAreas`, async via `node_service_`, same pattern as `TagSearchIndex`) and filters the journal by adding the chosen area node via `EventTableModel::AddFilteredItem` (whose `IsEventShown` already matches events whose containing node is filtered); "All areas" clears it. The browse roots at **`DataItems` (`data_items::id::DataItems`, "Все объекты")** — the same root the object tree uses (`object_tree_view.cpp`) — and excludes leaf `DataItemType` children, so it lists the operator-facing groupings (`ObjectsFolder` is one level too high — it holds the standard OPC folders + the "Все объекты"/"Все оборудование" containers, not the areas). And a **Period** dropdown of the quick-pick ranges (15 min / Hour / Day / Week / Month, mirroring the toolbar `ID_TIME_RANGE_*` values) → `EventView::SetTimeRange`, reflecting the current `GetTimeRange()`. `MakeEventFilterBar` now takes an `EventFilterBarContext` struct. Period→index mapping unit-tested (`EventFilterBarPeriodTest`, 4 cases in `client_events_qt_unittests`); the area enumeration is verified at runtime against the real `v1::NodeServiceImpl` over the fixture (`ScreenshotGenerator.EventFilterBarEnumeratesAreas` — non-empty, every entry a non-leaf grouping, areas + excluded leaves == all `DataItems` children). Note: an arbitrary **Custom…** range is *not* offered in the bar because calling the time-range dialog from `client_events` would form a link cycle (`client_component_time_range` already depends on `client_events`); Custom stays reachable via the existing toolbar/context-menu `ID_TIME_RANGE_CUSTOM` command, and a matching custom range still reflects when it equals a preset. **Context menu now cross-platform** (`modules/events/event_menu_model.{h,cpp}`): `EventView` builds its own `aui::SimpleMenuModel` (Acknowledge, Acknowledge All, **Unacknowledged Only** toggle, Severity ▸ All/Custom — checkable/enabled state preserved) backed by its `command_registry_`, shown via `ShowPopupMenu(&model, /*resource_id=*/0, …)`. This **removes the Windows-only `IDR_EVENT_POPUP` blocker** that previously kept the acknowledge/severity/unacknowledged actions from appearing off-Windows (`main_window_qt.cpp`'s `#else` fell back to the generic `context_menu_model_`). New command id `ID_UNACKNOWLEDGED_ONLY` (34124); unit-tested (`EventMenuModelTest`, 4 cases). `OpenedView::ShowPopupMenu` now treats a supplied AUI `merge_menu` as authoritative (no Windows default-resource substitution), the reusable seam for converting the other resource-menu views. **Remaining resource-menu views now converted** (follow-up to the event view): `table`/`watch`/`favorites`/`sheet` each build their own Qt-free `aui::SimpleMenuModel` (`modules/<view>/*_menu_model.{h,cpp}`, backed by the view's `command_registry_`, unit-tested — 14 cases across `client_component_{table,watch,sheet}_qt_unittests` + `client_favourites_qt_unittests`); `config-tree`/`timed-data`/`portfolio` carried only the dynamic `<Item>` placeholder (no view-specific static items) so they switch to the generic cross-platform context menu (`resource_id=0`, no model); `graph`/`node_table` keep the generic default (all their commands are centrally registered Actions already surfaced by the dynamic menu). To avoid double-listing centrally-registered commands (e.g. Delete/Pause/Save As/Color) that the appended generic `ContextMenuModel` also shows, `BuildDefaultPopupMenu` now suppresses generic entries already present in the caller's `merge_menu` (also removes the event view's latent Acknowledge-All duplication). Orphaned `IDR_{EVENT,TABLE,LOG,FAVOR,PFOLIO,SHEET}_POPUP` resource menus + defines removed. **The journal now reads as an alarm surface** (the mockup's severity coding, minus its chrome): under the reshell theme the Severity cell **names** the alarm band as well as numbering it ("Критично 80" / "Предупреждение 60") and an unacknowledged row's Acknowledge-Time cell says **"— pending —"** instead of sitting blank — so severity and "nobody has responded yet" stop depending on the row colour alone (cross-cutting rule: colour is never the only signal). The band names come from the new Qt-free `modules/events/event_severity.{h,cpp}` (`SeverityLevelForEvent` moved out of `main_window/status_bar/`, plus `SeverityLevelLabel`/`EventSeverityLabel`), which the status strip, the KPI tiles and the journal now all share — one place spells a severity, one place thresholds it. The themed Severity column widens 45→165px to fit the name; the legacy journal is unchanged in both respects. Fixed a latent sort bug while there: the Severity column compared **cell text**, so "100" sorted before "80"; `CompareCells` now compares the severity itself. Unit-tested (`EventJournalAlarmSurfaceTest`, 6 cases: legacy-unchanged, themed naming, routine severity unnamed, pending vs. acknowledged cell, numeric sort) and validated headless as `events-alarm-surface.png` under `--theme=dark`, a second `EventJournal` capture alongside the untouched legacy `events-log.png`. **Alarm chrome landed (dots + footer + Acknowledge-all)**: the journal gained the mockup's leading **unacknowledged-dot column** (`EventColumnUnacked`, appended id / shown first under the reshell theme only — a severity-coloured ● per pending row, sortable, excluded from CSV/Excel/print via the view's `export_columns_`), and an **alarm footer** (`modules/events/qt/alarm_footer.{h,cpp}`) with the displayed-backlog summary — `EventTableModel::GetAlarmSummary()` (count + highest severity, collapsed repeats included) rendered by the pure `events::AlarmSummaryLabel` ("Unacknowledged: N · highest: Critical 80", calm zero state) — plus an **Acknowledge All** button running the view's existing `ID_ACKNOWLEDGE_ALL` command with its enablement; the strip refreshes on every model notification. **Fixed while there:** a live unacknowledged alarm that also appeared in the read history rendered twice (once per surface) — historical copies now dedupe by event id against the current set. The fixture seeds two pending alarms (`"acknowledged": false`, honored by `LocalHistoryService::LoadFromJson` — a common-repo change) so `events-alarm-surface.png` renders the full chrome (dots, — pending — cells, footer "Не квитировано: 2 · наивысшая: Критично 80", enabled button); the capture is now **published** and embedded in the manual's workbench page (RU+EN + events-page pointers). Unit-tested: `AlarmSummaryLabel` (3), `EventAlarmChromeTest` (summary/calm/dot/dedupe, 4), `AlarmFooterTest` widget (3), `LocalHistoryService.LoadFromJsonHonorsAcknowledgedFlag`. *Remaining:* the Areas sidebar with per-area counts and the per-event Inspector (message, details, history timeline, Go-to-source); the severity bar glyph. | `modules/events/` | 0.2 | Alarm strip shows only actionable items; journal keeps full history + filters; ack-all works. |
| 2.3 | **Severity tiles / KPI counts** — ✅ *landed (opt-in)*. **Logic:** Qt-free `modules/events/severity_tiles.{h,cpp}` — `CountSeverityTiles()` aggregates an alarm set into critical/warning/unacknowledged, and `BuildSeverityTiles()` returns display-ordered tiles (most severe first) coloured from the severity single source (0.2), with unacknowledged carrying no severity colour since it is a workflow state. Counts *active* alarms only — a cleared-but-unread alarm leaves the severity tiles and stays under unacknowledged (ISA-18.2 separates alarm state from acknowledgement). 7 unit tests. **Widget:** `modules/events/qt/severity_tile_strip.{h,cpp}` renders those tiles — calm (muted, plain) at zero, bold + severity-coloured while live, the caption always naming what the number means so colour is never the only signal. Opt-in: `MakeSeverityTileStrip()` returns nullptr under the legacy theme. It replaces the ad-hoc Critical/Warning labels previously inlined in `CreateContextBar`, so the context bar now also carries the **Unacknowledged** tile and takes its order/colouring from the shared builder. 4 widget tests (legacy-gating, tile build, live refresh as alarms arrive/clear, calm-vs-asserted). **Live counts:** `EventStatusProvider` now aggregates once through `CountSeverityTiles()` (new `GetTileCounts()`); the existing `GetSeverityCount()`/`GetAlarmCount()` — already surfaced on `StatusBarModel` and refreshed on `PanesChanged` — are views onto it, so the tiles and the activity-rail badge share one tested semantics. 3 provider tests over `MockNodeEventProvider`. Russian in `client_ru.ts`; validated headless as `severity-tiles.png` (standalone `SeverityTiles` capture, seeded 1 critical / 3 warning / 7 unacknowledged) plus `client-window.png` under `--theme=dark` showing all three tiles in the real context bar. *Remaining:* the client's event model has **no cleared-but-unread state** — `NodeEventProvider` retains only unacknowledged events and carries no condition-active flag — so live counts cannot yet distinguish an alarm that returned to normal from one still active; the unacknowledged tile therefore equals the total. `CountSeverityTiles`'s active/cleared split is exercised by tests only until the provider gains that state. The Overview page (2.1) still leans on the context bar for its KPI strip rather than hosting its own. | `modules/events/`, `aui/` | 0.2 | Counts update as alarms arrive/clear. |
| 2.4 | **Trend workspace + polish**: chart-primary tab with series chips, cursor readout, min/max/avg value grid, and a series inspector (colour, own-pane, limits, annotations); crisp thin grid + limit markings; embedded mini-trend option beside values (`trend.html`). 🔷 *in progress*: (1) **themed crisp grid landed** — under the opt-in reshell theme `MetrixGraph` now overrides its palette background with the theme `surface` token, so the trend reads as a dark control-room surface (dark fill, subtle token grid, light axis labels) instead of the base `Graph`'s hardwired white; the base derives text/grid pens from the palette so one override themes fill+grid+axes without touching the `graph_qt` submodule. Gated on `GetSeverityTheme() != kLegacy`; legacy chart unchanged. Applies to both the main-window Graph tab and the standalone capture. Validated on the real widget (`--theme=dark` capture of `graph-cursor.png`). (2) **limit markings wired** — `modules/graph/limit_markers.{h,cpp}` adds a pure, unit-tested `ComputeLimitMarkers` that classifies the AnalogItem Lo/LoLo/Hi/HiHi bands by design-ramp severity (outer = alarm, inner = warning); the `graph_qt` `GraphLine` gained a per-band `LimitStyle` (severity colour + caption, default = the historical series-coloured dashed line) and `MetrixLine::UpdateLimitStyles` drives it from `aui::SeverityColor` (opt-in — returns nothing under `kLegacy`, so the default look is unchanged). Fixture support: `limit_*` property names in the generator + limits on `TIT.200`. Now rendered end-to-end: the graph capture makes each graphed node resident (`FetchGraphNodesResident`) so limits/EU range/current value fetch — `limits-chart.png` shows the four severity-coloured dashed bands with captions (Alarm/Warning hi/lo) on `TIT.200`. (3) **series inspector landed** — `modules/graph/series_inspector.{h,cpp}`: a themed per-series panel (trend.html) with colour swatches (active ringed; a click recolours the line), appearance toggles (own-pane / dots / stepped / Y-axis), the configured limit bands (severity-coloured, via `ComputeLimitMarkers`) and source (node / quality). `GraphView` surfaces it beside the chart under the reshell theme (opt-in; legacy returns the bare graph), refreshed on pane/line selection. Validated by a standalone `series-inspector.png` capture against the mockup. *Known: `TIT.208`'s narrow EU range vs its wide synthetic history makes the frequency pane's axis degenerate once EU is applied — fixture tuning follow-up.* | `modules/graph/`, `graph-qt`, `main_window/` | 1.4, 4.3 | Trend tab matches `trend.html`; overview trend matches `operator-shell`; limits visible; cursor grid live. |
| 2.5 | **Alarm flood affordances** — ✅ *landed (opt-in)*: `main_window/alarm_flood.h` holds the pure policy (`kAlarmFloodThreshold` = 10, `IsAlarmFlood`, unit-tested truth table incl. "exactly at the threshold is not a flood"), and `CreateContextBar` renders a **flood pill** — a single prominent critical-filled state with the count, hidden until the threshold is crossed and refreshed with the status-bar model — so a flood reads as one escalated state rather than a number the operator has to notice. Russian in `client_ru.ts`. **Trigger is deliberately count-based**: ISA-18.2 defines a flood as >10 alarms *in 10 minutes* ([`principles.md`](principles.md) §3), but the client has no arrival-rate signal — `NodeEventProvider` exposes the current unacknowledged set, not an event-rate history — so the criterion is a standing backlog of >10 unacknowledged alarms. That catches the state the operator is actually in (buried) rather than how fast they got there, and needs no new plumbing; a true rate window would mean timestamping arrivals client-side or a server-side rate signal, and is not planned. **Grouping landed for the journal's history**: `modules/events/event_grouping.{h,cpp}` (Qt-free, unit-tested) collapses repeats of one alarm — same source node *and* message, deliberately **not** severity, so a condition that escalates does not split back into two rows — into a single row represented by the newest occurrence and carrying its count in the Message cell ("КП-02: обрыв связи ×7"), via `FormatGroupedMessage`. `EventTableModel` decides on each rebuild whether to collapse, from the same `events::IsAlarmFlood` threshold the shell pill uses, so grouping engages exactly while the operator is buried and expands again once the backlog is acknowledged down — it is a property of the situation, not a mode to leave on. `alarm_flood.h` moved `main_window/` → `modules/events/` (namespace `events`) so the pill and the journal cannot disagree about what a flood is. 13 unit tests. **The live rows collapse too**, which completes the acceptance: an arrival folds into the group it repeats (`FindAlarmGroupRow`, taking over as the displayed occurrence when it is the newer one) instead of growing the list, and an acknowledgement drops *that one occurrence* from its row (`RemoveOccurrence`, promoting the newest remaining when the acknowledged one was on display) instead of removing the whole row and taking unacknowledged alarms with it. This had to be incremental, not a deferred rebuild: `EventStorage::Update` **extracts** an acknowledged event from its map before notifying, so the pointer a collapsed row holds dies with that call — the earlier deferred-rebuild attempt would have left dangling pointers in `repeats` as well as breaking `CurrentEvents_NewUnackedEvents`/`_AckEvents`. Whole-row acknowledgement in the journal now copies **every** occurrence into the history (not just the representative), for the same reason. Grouping engages and releases on its own as the backlog crosses the threshold (`RegroupIfFloodChanged`). 20 unit tests over the lifecycle: collapse, fold-in, a distinct alarm mid-flood keeping its own row, single-occurrence ack, representative promotion, whole-group ack, and re-expansion. **Fixed while there:** acknowledging a multi-row selection walked row indices as it acknowledged, and a regroup renumbers rows mid-loop — so it could acknowledge an alarm the operator never selected, or run off the end. `AcknowledgeRows` now resolves the whole selection to event ids up front (`EventView::AcknowledgeSelection` passes the selection in one call); the regression test fails against the old shape. *Remaining:* no headless capture of either the pill or a collapsed row — the fixture holds no unacknowledged alarms, and seeding a flood into it would rewrite the published `events-log.png`; both are covered by unit tests instead. **Exports and printouts expand the groups** (`modules/events/expanded_event_model.h`): grouping is how a flood is *displayed*, but a record of the journal is evidence of what happened, so `EventView::GetExportData` (CSV, Excel, and print, which share the same `TableExportData` seam) hands over an `ExpandedEventModel` — a read-only adapter presenting one row per occurrence via `EventTableModel::GetOccurrenceCell`, each with its own timestamp and no "×N". The generic export machinery is untouched — it walks any `TableModel` by row and knows nothing about groups — so the other exporting views (table / watch / timed-data) are unaffected. Rendering was split into a per-event `GetEventCell`, which also fixed a latent inaccuracy: the User / Acknowledged-By cells read the row's `NodeRef`, which belongs to the occurrence on display, so a collapsed repeat with a different user now resolves its own. 3 tests (every occurrence present and uncounted, per-occurrence timestamps, expansion a no-op when nothing is grouped). **The CSV dialog makes it a choice**: `TableExportData` now carries both forms (`model` as displayed, optional `expanded_model`), `CsvExportParams` gains a persisted `expand_groups` (default on), and the dialog shows an **"Expand grouped rows"** checkbox **only when the data being exported has groups** — every other view would see an option with no effect. Excel and print have no options dialog and take the expanded form. 3 dialog tests (hidden-and-preference-untouched when nothing groups, offered + default-on + remembered when it does). That lupdate pass is now done: the dialog's other 12 strings were sitting in `client_ru.ts` as `vanished` under the empty context — translated years ago, but dropped by `lrelease`, so the dialog ran in English. They are restored under `CsvExportDialog` (the context `uic`/`tr()` actually look up), verified by decompiling the built `.qm` with `lconvert`: the shipped catalogue went 385→397 messages and now carries all 14. The literal delimiter (`,`) and quote (`"`) combo items are deliberately left out — `accept()` parses those as single characters, so translating them would break the dialog. **Architecture — grouping stays in `EventTableModel`, not a wrapper**: a read-only design investigation weighed refactoring grouping into a proxy `TableModel` wrapping a plain non-grouping model (the way `ExpandedEventModel` wraps for exports). Verdict: **net loss**, so it was not done. The pure decision rule is *already* extracted (`event_grouping.{h,cpp}` + `alarm_flood.h`, Qt-free, unit-tested); the stateful part manipulates the `scada::Event*` pointers that only `rows_` holds, and a proxy cannot see those at ack time — the base `aui::TableModel` signals carry only `(first, count)`, so a proxy would have to **mirror** the base's `row→event` map and re-derive `RemoveOccurrence`/`FindOccurrenceRow` in its `ItemsRemoving` handler, *before* `EventStorage::Update` frees the extracted event. That relocates the pointer-lifetime hazard rather than removing it, and adds an index-mapping + 6-signal-translation layer that aui has no framework for (its only proxy, `MirrorTableModel`, is a row-count **bijection** that grouping breaks). A `CollapseStrategy` injection was likewise rejected — it would abstract only the already-pure ~5% while the coupled pointer surgery stays put. **Revisit trigger:** two live views over one model instance that must disagree on grouping at the same time (e.g. a docked raw-journal pane beside a flood-collapsed one); nothing needs that today, and even then prefer promoting `ExpandedEventModel`'s regroup-on-`ModelChanged` snapshot style over a fully incremental proxy. *Remaining (validation only):* no headless capture of the flood pill or a collapsed row — the fixture holds no unacknowledged alarms and seeding a flood would rewrite the published `events-log.png`; the whole lifecycle is covered by unit tests instead. | `modules/events/` | 2.2 | More than 10 unacknowledged alarms shows a grouped indicator, not a raw scroll. |
| 2.6 | **Substation display frame**: wrap the Modus/Vidicon renderer as a workspace tab — `Live` indicator, zoom/fit/export, hotspot breadcrumb, selection → Inspector, bay Measurements + Recent-events strips. Geometry unchanged. 🔶 *in progress*. **Display-frame chrome landed** (`modules/display_frame/qt/display_frame.{h,cpp}`, new `client_display_frame` module): a cross-platform (`UI_QT`) `DisplayFrame` `QWidget` wraps the renderer in the reshelled display toolbar — a **Live** indicator pill, a **hotspot breadcrumb** (the display title), and a **zoom / fit / 100% / export** control that hosts the renderer in a scrollable viewport (zoom scales the rendered page; **geometry unchanged**). Opt-in via `WrapDisplayInFrame`, gated on `scada::aui::GetSeverityTheme() != kLegacy`; the legacy build returns the bare renderer untouched. Wired into `VidiconDisplayNativeView::Init` (the cross-platform VDS host — `VdsRuntimeWidget`; builds on macOS while `BUILD_MODUS/VIDICON` ActiveX stays Windows-only). Fit keys off the scroll **viewport's** resize (event filter), not the frame's, so it settles after layout. Zoom maths are pure free functions, unit-tested (`DisplayFrameZoomTest`, 8 cases). Russian labels in `client_ru.ts`. **Selection → Inspector already flows**: the display views publish the hit-tested `data_source`→`NodeId` through the shared `SelectionModel` (`SelectTimedData`), which the shell routes on active-view change — the Inspector *panel* itself is not yet a reshell surface (selection currently drives command enablement). **Validated on macOS**: a new cross-platform screenshot capture (`tools/screenshot_generator/display_capture.{h,cpp}`, `CaptureDisplay` + `Display` manifest spec + `fixtures/substation.vds`) renders the framed VDS display headless (Qt `offscreen`, `--theme=dark`) — real single-line diagram + full chrome. **Bay strips landed**: below the diagram the frame now hosts a two-panel `QSplitter` (built only when live-data sources are supplied) — a **Measurements** watch (`Signal`/`Value`/`Updated`) fed by `TimedDataSpec`, into which each selected element is pushed live (`ShowMeasurement`, mirrored from the selection callback), and a **Recent-events** strip (`Severity`/`Time`/`Object`/`Message`) reusing the event surface via `NodeEventProvider::unacked_events()` (observer-refreshed; source names via `NodeService::GetNode`). Both providers are threaded through `VidiconDisplayNativeViewContext` from the `ControllerContext`. The events strip is unit-tested with a fake provider (`DisplayFrameStripsTest`, incl. gating), and `DisplaySeverityBandFor` is pure-tested; the whole client links with the wiring. (Note: seeded-data strip *screenshots* are blocked by a pre-existing generator full-run panic at `property_service.cpp:67` — the strips are validated by the widget tests instead.) **Inspector panel landed** (`modules/inspector/qt/inspector_panel.{h,cpp}`, new `client_inspector` module): a reshell right-hand `QDockWidget` (`InspectorPanel`) that reflects the **active view's `SelectionModel`** — header (title + node-id/formula subtitle), a state-hero (big live value + a quality pill coloured `good`/`bad`), a Measurements section (Updated timestamp), and a **Control** action that reuses the selection-scoped `ID_WRITE` command (the existing two-stage confirm) with live enablement. Built opt-in in the Qt `MainWindow` (gated with the other reshell chrome on `GetSeverityTheme() != kLegacy`) and refreshed from `OnSelectionChanged()` — so clicking a display element (which publishes a `TimedDataSpec` on the view's `SelectionModel`) now **fills the Inspector**. Quality-band mapping is pure-tested (`InspectorQualityBandTest`); the panel is widget-tested (`InspectorPanelTest`: empty-state, element readout, control enable/disable, clear — 6 cases total), and the whole client links with the wiring (`nm` confirms the symbols in the app). The value **ticks live** while the element stays selected: the panel copies the selection's connected `TimedDataSpec` (which shares the underlying `TimedData`, so the copy is a live observer) and owns its `update_handler`. Russian labels in `client_ru.ts`. *Remaining:* the bay-scoped Navigate links; and Modus ActiveX equipment colouring is Windows-only (validate there). | `modules/display_frame/`, `modules/vidicon/display/`, `modules/modus/`, `main_window/`, `modules/events/` | 1.1–1.5, 4.3 | Matches `substation-display.html`; clicking an element selects it and fills the Inspector; controls open the two-stage confirm. |
| 2.7 | **Equipment-state colouring** applied by the renderer via `--sl-*` tokens (closed/open with shape, energized/de-energized, bad-quality). 🔶 *investigation*. **Finding:** the cross-platform VDS renderer (`designer/runtime/vds_runtime.cpp`, `tc_vds_runtime`) is today a **static** painter — `RenderBgra` paints the authored document via `PaintDocument`; `invalidate_data_source(value, quality)` is a **no-op** (value/quality dropped) and there is no data binding or state→colour mapping at all. So equipment-state colouring on the cross-platform path is a genuine **`designer/` runtime feature** (per-data-source state/value/quality store + a renderer state→colour pass + a palette-injection ABI so the client passes the `sl_*` token colours), not client-side glue; the `sl_*` tokens live in `aui/qt/theme_qt.h` (`ThemeTokens`) ready to feed in. **Client-side mapping landed** (`modules/display_frame/qt/single_line_style.{h,cpp}`): a pure, renderer-agnostic `SwitchStyle`/`ConductorColor` maps `{SwitchState, Energization, SignalQuality}` → an `sl_*` token colour **and** a `DeviceShape` (filled = closed, hollow = open) — closed→`sl_closed`, open→`sl_open` (neutral), energized→`sl_live` (amber, asserted ≠ alarm-red), de-energized→`sl_energized`, bad-quality overrides to `bad` + indeterminate shape. Unit-tested (`SingleLineStyleTest`, 9 cases incl. the "open/energized is not alarm-red" and "state distinct by shape AND colour" invariants). **Remaining (designer/):** an ABI-bumped `designer/runtime` palette + per-data-source state/value/quality channel so `RenderBgra` actually applies these colours to bound shapes (committed separately in `designer/`); and the Modus COM push on Windows. Modus ActiveX colouring is set via its Russian-named COM `OLESTR` params (Windows only; do **not** rename). | `designer/runtime/`, `modules/modus/`, `modules/vidicon/display/`, renderer | 0.1, 0.2 | Switching-device state reads by shape **and** colour; energized ≠ alarm-red; matches `design-language.md`. |
| 2.8 | **Table / Watch workspace**: live+historical operator grid — NodeId/formula rows, live values, quality marks, per-row sparklines, timestamps; toolbar (add item/device, remove, move, sort, Live/At-time, add-to-graph, CSV, print); Qt-shaped row context menu; row inspector (`table-watch.html`). 🔶 *in progress (opt-in):* **quality marks landed** — under the token theme the Table grows a good/uncertain/bad **Quality** column (translated label + `QualityColor`) and token-colours the row's data cells; `QualityFromQualifier` unit-tested; validated on the real Qt table (`--theme=dark` shows the column, legacy hides it). **Screenshot capture** now seeds the Table with multiple data items so `table.png` renders a populated grid. **Sparkline geometry** (`ComputeSparklinePoints`) landed + unit-tested. **Toolbar landed** (`modules/table/qt/table_toolbar.{h,cpp}`, opt-in): a themed command strip above the grid — **+ Add signal** (opens the editor on the trailing "Enter expression" row), **Delete / Move up / Move down**, checkable **Sort** keys (Name / Channel), and the shell commands **To graph / CSV / Print** — each button executing through a `resolve_command` seam (the view's own `CommandRegistry` first, then the shell) and tracking availability (hidden when unresolved) + enablement via `Refresh()` on selection changes / show / after each press (a queued re-read covers tab-activation racing the active-view update). The seam behind it is new **`ControllerDelegate::ResolveViewCommand`** → `OpenedView` → `BaseMainWindow::ResolveViewCommand` — the *same* resolution (Global/Selection/OpenedView/Controller contexts + router fallback) the main-window toolbar, palette and inspector controls use, now also available to any view-embedded control; the six `main_window_qt.cpp` call sites were folded onto it. The row commands gained real `enabled_handler`s (no selection / entry-row ⇒ Delete·Move disabled — greying the context menu too, where they previously always read enabled). Unit-tested (`TableToolbarTest`, 5 cases: legacy-gating, show/hide-by-resolution + execute, enablement refresh + disabled-no-execute, sort-check mirroring, add-signal) and validated headless (`table.png` under `--theme=dark`: toolbar row with Добавить сигнал / Удалить строку / Сместить вверх·вниз / Упорядочить Имя·Канал; Delete/Move correctly disabled with no selection). *Shell-command buttons (To graph/CSV/Print) hide in the capture because the captured view is never the shell's active view — exactly the resolution contract; their visibility on a live active table follows the same path the E2E already exercises for the Table context menu (`ID_PRINT`/`ID_EXPORT_CSV`).* **Row → Inspector landed**: single-row selection already published the row's `TimedDataSpec` on the view's `SelectionModel` and the shell routes it to the 2.6 `InspectorPanel`; the panel's guard dropped node-less specs, so **formula/expression rows** (live value, no backing node) showed nothing — it now shows any spec with a node *or* a formula (folder/object selections still clear). Regression-tested (`InspectorPanelTest.FormulaRowSelectionFillsTheInspector` + `DataLessSelectionShowsTheEmptyState`, over a fake `TimedDataService`). **Sparkline column landed** (completing the mockup's row anatomy): under the token theme the Table grows a **Trend** column — a per-row mini-trend painted by `modules/table/qt/sparkline_delegate.{h,cpp}` (QStyledItemDelegate; accent-coloured antialiased polyline over the cell background) from the landed `ComputeSparklinePoints` geometry plus a new pure `NumericSeries` extractor (numeric + coercible bool samples in, null/text skipped — no invented zeros). Each reshell row's `TimedDataSpec` now observes a **trailing 1 h history window** (`kSparklineWindow`, `TableRow::SetFormula`; `update_handler` repaints on history arrival); the legacy grid stays current-only. Enabled by two harness fixes: (1) the **generator clock anchor** — the screenshot fixture freezes `base::Time::Now()` at the fixture `now` (`ScopedMockClockOverride` + shared `FixtureNow`), so live windows overlap fixture history and *every* capture timestamp is deterministic (documented in `docs/screenshots.md`); (2) `LocalHistoryService` (scada-common) now synthesizes its 48-point series **across the requested range** (a 24 h graph request keeps the exact historical 30-min spacing; a 1 h sparkline window gets a full series) — unit-tested in common. 5 new client tests (NumericSeries ×2, delegate paint ×2, reshell-window regression) + all suites green; validated headless — `table-workspace.png` shows live sparklines in every row ("Тренд", Russian label added by hand) and is republished to the manual (workbench.md RU+EN describe the column). *Remaining:* Live/At-time toggle (the Table model is live-only — no historical mode to toggle yet); Qt-shaped context-menu parity (disabled + reason — enablement now real, reasons not surfaced); a sparkline for the Watch view once it gains a value column. | `modules/table/`, `modules/watch/`, `modules/sheet/`, `main_window/` | 0.1–0.4, 4.3 | Matches `table-watch.html`; rows edit/move/delete/sort; menu mirrors the Qt structure with unsupported items disabled + reason; selection fills the Inspector. |

## P3 — Dialog theming (retire native OS widgets)

| # | Item | Touches | Dep | Done when |
|---|---|---|---|---|
| 3.1 | **Login** — 🔶 *theming validated*: under the opt-in theme the real login dialog already recolours cleanly (charcoal-blue surfaces, rounded token fields, accent focus + primary OK) in dark & light — confirmed on the real Qt widget via `client_screenshot_generator --theme=…`. *Remaining (optional, its own slice):* the richer `login.html` treatment (brand lockup + read-only system preview), which needs `.ui` restructuring. | `modules/login/qt/` | — | No native chrome; anonymous + auto-login intact; validated by real-widget screenshot. |
| 3.2 | **Control/Write** — ✅ *done*: dialogs theme cleanly under the opt-in theme (validated on real widgets), **and** the control confirmation is now a present→command review with an irreversibility warning (principle §7) instead of a bare "Switch X to Y?" — control commands still confirm before the operate stage. Fixed a latent dangling-`string_view` in the confirmation prompt while there (message/title now owned by the awaiting coroutine). Unit-tested (`WriteModelTest.ControlCommandConfirmationReviewsPresentAndCommand`). *Optional later:* a bespoke diff dialog with a danger-styled Send button (`control-command.html`) in place of the message box. | `modules/write/` | — | Present→command review shown before control commands; unit-tested; theming validated by real-widget screenshot. |
| 3.3 | **Limits** — ✅ *validated*: warning/alarm limit editor (group boxes + fields) themes cleanly under the opt-in theme on the real widget (`--theme=dark` capture of `limits.png`), no per-dialog code. | `modules/limits/qt/` | — | Themed limit editor; validated by real-widget screenshot. |
| 3.4 | **Remaining modals** (change_password, select_item, time_range, about, create/multi-create). | `modules/*/qt/` | 0.4 | No modal renders native/unstyled. |

## P4 — Explorer & Inspector depth

| # | Item | Touches | Dep | Done when |
|---|---|---|---|---|
| 4.1 | **Explorer status dots** — ✅ *landed (opt-in)*: each object-tree node with a live value shows a quality dot (good/uncertain/bad) composed before its icon, coloured from the severity single source (`QualityColor`) via a new `TreeModel::GetStatusColor` hook fed by `VisibleNodeModel` (`IsBad`/`IsAlerting`). Folders/objects get none. Empty under the legacy theme. Validated on the real object tree (`--theme=dark`); `QualityColor` unit-tested. Inline live values already exist (Value column). | `modules/configuration/objects/`, `aui/qt/tree_model_adapter`, `aui/` | 0.1–0.3 | Each node shows quality dot; validated by real-widget capture. |
| 4.2 | **Explorer filter field** — ✅ *landed (opt-in)*: a generic `aui::Tree::SetFilterText` (recursive, case-insensitive substring on column 0; ancestors of a match stay visible) drives the tree's `QSortFilterProxyModel`; `ConfigurationTreeView::Init` wraps every Explorer tree (Object / Nodes / Hardware / FileSystem) in a themed "Filter" field above it under `#if defined(UI_QT)` + the reshell theme (legacy keeps the bare tree; tree ownership transfers into the container unchanged). Filtering sees already-fetched rows (a lazy subtree filters as it expands). Unit-tested (`TreeTest.SetFilterText*`: hide/clear, case-insensitive, ancestor-keeping — 3 cases); Russian in `client_ru.ts`. | `modules/configuration/`, `aui/qt/` | 1.1 | Type-to-filter narrows the tree. |
| 4.3 | **Inspector panel**: big readout, measurements-with-limits, controls (disabled+reason). | new `main_window/qt/inspector`, `modules/write`, `modules/limits`, `modules/node_properties` | 3.2, 3.3 | Selecting a node fills the inspector; controls open themed dialogs; admin-gated actions disabled with reason. |
| 4.4 | **Sidebar extra panes**: Watchlist, Favorites/Portfolio as Explorer siblings. | `modules/watch`, `modules/favorites`, `modules/portfolio` | 1.1 | Panes switch within the sidebar host. |

## P5 — Engineering surfaces & web parity

| # | Item | Touches | Done when |
|---|---|---|---|
| 5.0 | **Configuration / device workbench** — ✅ *landed (opt-in, slice)*: `modules/parameter_form/` — Qt-free `parameter_staging.{h,cpp}` (dirty tracking, Revert/Apply gating) + `qt/device_parameter_form.cpp`, with a limits grid tab and device-limits browse wired through the controller. Unit-tested (`parameter_staging_unittest`, `device_parameter_form_unittest`); validated headlessly (`device-diagnostics.png`, `config-parameters/-address-map/-limits` under `--theme=dark`). *Remaining:* the address-map tab is the existing view rather than a reshelled editor; no cross-tab Revert/Apply banner. Original scope: hardware/device tree, tabbed device parameter editor (protocol fields, address map, limits), Revert/Apply, live device-diagnostics inspector (`config-workbench.html`). 🔶 *in progress*. **Device-diagnostics inspector landed** (`modules/device_diagnostics/`, new `client_device_diagnostics` module): the mockup's right region as a cross-platform (`UI_QT`) `DeviceDiagnosticsPanel` `QWidget` — a link-status **hero** (up / down / disabled, via the pure unit-tested `DeviceLinkBandFor`), a live **Counters** section (Messages/Bytes RX·TX, Interrogations, Clock syncs) and a **Metrics trend** action. `ShowDevice` resolves the device's diagnostic child variables — the `DeviceType` data variables `Online`/`Enabled`/`MessagesIn`/… — primarily by the type's aggregate declaration (`device[DeviceType_Online]`) with a browse-name fallback, then reads each reading from its live `TimedDataSpec` **falling back to the node's Value attribute** so the hero + counters populate immediately on selection (and offscreen), rather than reading blank/"Disabled" until the first monitored-item update. Opt-in via `MakeDeviceDiagnosticsPanel` (gated on `GetSeverityTheme() != kLegacy`; returns null in the legacy look). Wired into the Qt `MainWindow` as a right dock **tabified with the Inspector**, filled from `OnSelectionChanged` when the active view's selection is a `DeviceType` instance (else cleared); the Metrics-trend action reuses the selection-scoped `ID_OPEN_DEVICE_METRICS` command (`SelectionModel` gained a `timed_data_service()` accessor so the shell can open selection-scoped specs). Russian labels in `client_ru.ts`. Unit-tested (`DeviceLinkStateTest` band truth-table + `DeviceDiagnosticsPanelTest` widget: empty/up/down/rows/action-enable — 8 cases) and validated headless against a fixture device (`device-diagnostics.png`, TS.103 seeded link-down + counters; standalone `DeviceDiagnostics` capture + manifest row). **Hardware-tree device-state status dots landed** (`modules/configuration/devices/device_state_color.{h,cpp}` + `HardwareTreeModel::GetStatusColor`): each device row in the Hardware explorer (Subsystems / `HardwareTreeView`) carries a good/uncertain/bad dot from its connection state — Online reads good, Offline bad, Disabled uncertain (present but not polled, not an alarm); channels and unknown-state devices get none — coloured from the severity single source (`scada::aui::QualityColor`, so empty under the legacy theme). State comes from the live `DeviceStateNotifier`, falling back to a `DeviceStateFromNode` Value-attribute snapshot (browse-name-tolerant read of Online/Disabled) so the dot resolves immediately on load and offscreen, before the first monitored-item update. Fixed a latent notifier bug while there: `CalculateDeviceState` returned **Offline** when no Online value had been delivered (a just-loaded / component-less device mislabelled as offline, which also masked the snapshot fallback) — it now returns **Unknown**, and the `GetIcon` device-state switch routes through the same accessor. `DeviceStateQuality` mapping unit-tested (`DeviceStateColorTest`, 4 cases); validated by `DeviceStateColorTest` plus the fixture seeds (КП-01 offline, КП-02 online, КП-03 disabled). Validated headless as `hardware-tree.png` under `--theme=dark` (filter field + КП-01 offline·red / КП-02 online·green / КП-03 disabled·amber). This is now capturable via the normal generator path after fixing its `--only` gate — see the generator note below; the plain (legacy) run still shows the tree without dots. **Explorer filter field landed** (backlog P4.2): a generic `aui::Tree::SetFilterText` (recursive, case-insensitive column-0 substring; ancestors of a match stay visible) with a themed "Filter" field wrapped over every Explorer tree in `ConfigurationTreeView::Init` (`#if defined(UI_QT)` + reshell theme); unit-tested (`TreeTest.SetFilterText*`, 3 cases). **Tabbed device-parameter editor landed** (`modules/parameter_form/`, new `client_parameter_form` module): the mockup's center workspace as `DeviceParameterForm`, a reshell form over the *existing* `aui::PropertyModel` (the same model the legacy property grid uses — so real properties, editors and the write path are reused). It renders one **subtab per top-level property group** (loose leaf properties fall under a "General" tab), a **Revert / Apply** bar, and a **dirty indicator**. Edits are **staged** in a pure `ParameterStaging` buffer rather than written live: typing records a pending edit (and typing the live value back clears it, so the form only reads dirty when something differs); **Apply** replays the staged edits through `PropertyGroup::SetValue` (the async write path) and clears the buffer; **Revert** discards them and restores the live values. Wired into `NodePropertyController::Init` under `#if defined(UI_QT)` + the reshell theme (via `MakeDeviceParameterForm`), so the admin-gated property view ("NewProps") presents the form; the legacy look keeps the property grid. Unit-tested (`ParameterStagingTest` 7 + `DeviceParameterFormTest` 6: tab-per-section, edit→dirty→Apply-writes, type-back-clears-dirty, Revert-restores). Russian labels in `client_ru.ts`. Field editors follow the model's `GetEditData`: text boxes, **editable dropdowns** (sync + async choices), **read-only** boxes, and **dialog-button** fields (`HandleEditButton`), each seeded with the live value and staged the same way (unit-tested: `DropdownPropertyBecomesAComboWithChoices`, `ChangingADropdownStagesThenRevertRestores`, `ReadOnlyPropertyIsNotEditable`). The reshell form is validated headless as `config-parameters.png` (a `NewProps` fixture spec on device `TS.702`, captured with `--theme=dark --only config-parameters.png`), plus the widget tests. **Address-map preview landed** (`modules/node_properties/device_address_map.{h,cpp}` + `DeviceParameterForm::SetAddressMap`): after the property-group tabs the form shows a read-only **Address map** grid — Signal · Type (TS/TI) · IOA · NodeId — sourced from the device's **transmission items** (`BuildDeviceAddressMap` browses `Organizes` children with a `HasTransmissionSource`, reading the source name/type and `TransmissionItemType_SourceAddress`) off the construction path via `CoSpawn` + a `QPointer` guard. `AddressMapRow` is a Qt-free struct so the browse stays out of the Qt layer; `SignalTypeTag` is pure/unit-tested (`SignalTypeTagTest`), and the grid tab is widget-tested (`SetAddressMapAddsAReadOnlyGridTab`, `EmptyAddressMapAddsNoTab`). Validated headless by a dedicated `config-address-map.png` capture (`TS.702`): the generator gained a `click_object` spec field that clicks a named child before grabbing, so the capture selects the address-map subtab (`addressMapSubtab`) and shows the populated grid — Signal / Type (TI) / IOA (1001…2002) / NodeId for the `TX_*` transmission items. (Resolving the IOA required fetching each transmission item's type chain so `operator[](SourceAddress)` maps the declaration to the instance property — a real sourcing fix the screenshot surfaced.) **Limits tab landed** (`modules/parameter_form/limit_row.h` + `DeviceParameterForm::SetLimits`; `modules/node_properties/device_limits.{h,cpp}`): a read-only **Limits** grid — Signal · LoLo · Lo · Hi · HiHi — of the device's analog data items, sourced by `BuildDeviceLimits` (browses `Organizes` children that `IsInstanceOf` `AnalogItemType`, type-chain fetched, reading the four `AnalogItemType_Limit*` bands; unset bands render empty) via a second `CoSpawn` + `QPointer` in the controller. The address-map and limits grids share a `MakeReadOnlyGrid` helper. Widget-tested (`SetLimitsAddsAReadOnlyGridTab`) and validated headless (`config-limits.png`, `TS.114`: `Активная мощность` 113/118/133/138, `Реактивная мощность` blank). **Diagnostics actions landed**: the device-diagnostics inspector's Actions row now carries **Metrics trend** (`ID_OPEN_DEVICE_METRICS`), **Reconnect** (`ID_ITEM_ENABLE` — re-enables the device; there is no distinct reconnect command, the link auto-reconnects) and **Open log** (`ID_OPEN_EVENTS`). The panel context switched from bespoke `on_metrics` callbacks to a generic `std::vector<DiagnosticAction>` (label + execute + is_enabled); the host wires each through the same `ResolveCommandHandler` selection path, so each button self-gates on its command's availability. Widget-tested (`ActionsRenderTrackEnablementAndExecute`); the `device-diagnostics.png` capture shows the full Actions row (График метрик / Переподключить / Открыть журнал). **This completes the config workbench (5.0).** | `modules/configuration/`, `modules/device_metrics/`, `modules/device_diagnostics/`, `main_window/` | Matches `config-workbench.html`; device params edit/apply; diagnostics inspector live; unsaved-changes guard. |
| 5.1 | **Users & access rights** admin — ✅ *landed (opt-in, slice)*: `modules/user_access/` — pure `user_access.{h,cpp}` RBAC logic + `users_grid.{h,cpp}` browse, with `qt/user_access_panel` (role/permission grants) and `qt/users_grid_panel` (grid + toolbar, actions menu surfacing Set Password / New→User). Unit-tested; captures `users-rbac.png`, `users-admin.png`. *Remaining:* inherited-vs-explicit grant distinction is not visualised; enable/disable and reset-password route to existing commands rather than a bespoke flow. Original scope: users grid + RBAC role/permission editor (inherited-vs-explicit grants), enable/disable, reset password, admin-gated; other node-table admin surfaces (Formats, Simulation, Historical DBs) adopt tokens (`users-admin.html`). 🔶 *in progress (opt-in)*. **RBAC inspector landed** (`modules/user_access/`, new `client_user_access` module): the mockup's right region as a cross-platform (`UI_QT`) `UserAccessPanel` `QWidget` — a **role pill** (Administrator / Operator / Observer) plus the three **permission rows** (view · control · configure) with granted state. The role and permission set are derived by pure, unit-tested logic (`UserRoleFor` / `UserPermissionsFor`) from the user's `UserType_AccessRights` Int32 **bitmask** (bit0 Configure ⇒ Administrator, bit1 Control ⇒ Operator, else Observer; view is always granted). `ShowUser` checks the selection `IsInstanceOf` `UserType`, then reads `AccessRights` off the node's Value attribute; the pill is coloured from the severity single source (`QualityColor`-adjacent tokens: admin=bad, operator=good, observer=subtle) so it is empty/neutral under the legacy theme. Opt-in via `MakeUserAccessPanel` (gated on `GetSeverityTheme() != kLegacy`). Wired into the Qt `MainWindow` as a right dock **"Access rights" tabified with the Inspector**, filled from `OnSelectionChanged` when the active selection is a single `UserType` instance (else cleared). Russian labels in `client_ru.ts`. Unit-tested (`UserRoleForTest` + `UserPermissionsForTest` role/permission truth-tables + `UserAccessPanelTest` widget: empty / admin pill+perms / observer pill / clear — 7 cases) and validated headless as `users-rbac.png` (standalone `UserAccess` capture, fixture user `USER.5` AccessRights=3 = full Administrator; makes the user resident so `ShowUser` resolves and reads `AccessRights`). **Users grid landed** (`modules/user_access/users_grid.{h,cpp}` + `qt/users_grid_panel.{h,cpp}`): the mockup's main region as a `UsersGridPanel` `QWidget` — a themed User / Role / Sessions table (role coloured admin=bad / operator=good / observer=muted from the same severity source as the inspector pill; Sessions = Single·Multiple from the pure `UserSessionsLabelKey(MultiSessions)`), with a count header + "editing requires the Administrator role" hint and disabled Add-user / Reset-password affordances. Rows come from `BuildUsersGrid`, which browses the Users folder's `UserType` instances (deduped by NodeId) and reads each user's `AccessRights` (→ `UserRoleFor`) + `MultiSessions`. **Wired into the real Users view**: `NodeTableController::Init` returns the panel (UiView == QWidget) instead of the generic grid under the reshell theme when the parent is the Users folder (`security::id::Users`); every other node table keeps the grid, and `Save` guards the now-null `grid_`. Selecting a row drives the shared `SelectionModel` (`selection_.SelectNode`), so the RBAC inspector fills exactly as from the grid. `client_component_node_table_qt` links `client_user_access_qt`. Russian labels in `client_ru.ts`. Unit-tested (`UserSessionsLabelKeyTest` + `UsersGridPanelTest`: populate+count / row-select emits UserActivated / empty clears — 4 new cases) and validated headless as `users-admin.png` (`reshell-theme`, fixture Users folder = 13 users; the min-rows check confirms the browse populated the grid). **Reset-password wiring landed**: a right-click on a user row, or the Reset-password toolbar button (enabled once a user is selected), emits `UsersGridPanel::ActionsMenuRequested`, which `NodeTableController` routes to `ControllerDelegate::ShowPopupMenu` — surfacing the **existing** `ID_CHANGE_PASSWORD` selection command (Set Password…, admin-gated on `SessionService::HasPrivilege(Configure)`) plus New/Delete for the selected user, reusing all the existing command + LocalEvents plumbing (no bespoke write path). Widget-tested (`ResetPasswordFollowsSelectionAndEmitsActionsMenu`: disabled without selection → enabled on select → button emits the actions signal). **Enable/disable is deliberately not offered:** the client `UserType` node model has no enabled/disabled attribute (only `AccessRights` + `MultiSessions` + profile fields), so account activation state is server-side and there is nothing to toggle from the client — the mockup's Status column / Account-enabled toggle are scope-noted, not faked. **Add-user wiring landed**: the Add-user toolbar button (always enabled) and a right-click anywhere in the grid emit `ActionsMenuRequested`, popping the view context menu whose CATEGORY_CREATE "New → User" entry runs the **existing** create command (`OpenedViewCreateCommand` → `PostInsertTask(UserType, Users-folder)`, gated on `Configure`) — reusing the real create path, no bespoke write. Widget-tested (`AddUserIsEnabledAndEmitsActionsMenu`). `users-admin.png` regenerated with the enabled Add-user button. **Sibling surfaces adopted**: Formats / Simulation / Historical DBs all render through the same generic `aui::Grid` (a `QTableView`), which now token-adopts the reshell theme in its constructor (`aui/qt/grid.cpp`, gated on `GetSeverityTheme() != kLegacy`, aui-owned tokens only): flat header sections (`surface_muted`/`fg_muted` + hairline), `border` gridlines, a soft-accent selection fill, and the previously-hardcoded black selection outline / expand handle now use `accent`. Legacy look is untouched; inline editing, sorting and the existing context menu all keep working (cell content still renders through the item delegate, which the stylesheet does not touch). *Validated with content — all three sibling surfaces:* **Simulation** (`admin-simulation.png`), **Formats** (`admin-formats.png`) and **Historical DBs** (`admin-historical-dbs.png`) now have populated themed captures. The `SimulationSignals` / `TsFormats` / `HistoricalDatabases` folders and their types (Function/Period/Phase/UpdateInterval; OpenLabel/CloseLabel/OpenColor/CloseColor; Depth/ItemCount/WriteValueCount/PendingTaskCount) were added to `scada_test_address_space.h`, with fixture instances (`SCADA.9301-9304` / `SCADA.9401-9404` / `SCADA.9501-9503`) in `screenshot_data.json` plus fixture LocalizedText handling for the format state labels, so each window renders the themed generic grid (flat header, hairline gridlines, dark cells) with real rows. This closes the sibling-surface adoption — the whole `NodeTableController` admin family (Users bespoke grid + Formats/Simulation/Historical DBs generic grid) is now themed and validated. No-regression confirmed via `address_space_unittests` (30) + `node_service_unittests` (64) + `timed_data_unittests` (45) + `client_screenshot_check_themed`. | `modules/node_table/`, `modules/change_password/`, `modules/create/`, `modules/user_access/`, `main_window/` | Matches `users-admin.html`; editing gated to Administrator; grids themed. |
| 5.2 | **Transmission-rules editor** — ✅ *landed (opt-in, slice)*: pure rule logic + `modules/transmission_rules/qt/transmission_rule_inspector` (rule editor inspector), unit-tested, capture `transmission-rule.png`. *Remaining:* the rules **grid** with live status/counts is not built — the inspector edits a rule, but there is no list surface yet; naming drift to *Editor* per the parity matrix. Original scope: rules grid (source → destination IOA, trigger, transform, live status/counts) + rule editor inspector; enable/disable, test-send, Revert/Apply (`transmission-rules.html`). 🔶 *in progress (opt-in)*. **Rule-editor inspector landed** (`modules/transmission_rules/`, new `client_transmission_rules` module): the mockup's right region as a cross-platform (`UI_QT`) `TransmissionRuleInspector` `QWidget`. For a selected transmission item it shows the **source** signal (name + TS/TI type tag pill), the **destination** endpoint (parent device) + **IOA**, and the **protocol** — all derived from what the client node model actually carries. Pure, unit-tested helpers (`TransmissionProtocolLabel` maps the item's type — Modbus / IEC 60870-5-104 / IEC 61850; `TransmissionSignalTag` maps the source data-item type to TS/TI; `TransmissionRuleSummary` builds "Ua → 2001"). `ShowRule` checks `IsInstanceOf(TransmissionItemType)`, resolves the source (`HasTransmissionSource`) and parent endpoint, and reads `SourceAddress` (Int32 IOA). The IOA field is **editable behind Revert/Apply** when the host supplies an `ApplyHandler` write path (staged dirty tracking; Apply fires the handler with the rule node id + new IOA); with no handler — the MainWindow shell, which has no `TaskManager` — the inspector is **read-only**. Opt-in via `MakeTransmissionRuleInspector` (gated on `GetSeverityTheme() != kLegacy`). Wired into the Qt `MainWindow` as a right dock **"Transmission rule" tabified with the Inspector**, filled from `OnSelectionChanged` on a single `TransmissionItemType` selection (else cleared). Russian labels in `client_ru.ts`. Unit-tested (`TransmissionProtocolLabelTest` + `TransmissionSignalTagTest` + `TransmissionRuleSummaryTest` truth-tables + `TransmissionRuleInspectorTest` widget: empty / fields+tag / edit→dirty→Apply-fires-handler / Revert / read-only-without-handler / clear — 12 cases) and validated headless as `transmission-rule.png` (fixture `TS.733` TX_Ua → Modbus IOA 2001; the capture makes the rule + its type chain, source and parent resident so `ShowRule` resolves everything and `SourceAddress` reads 2001). **Scope note:** the mockup's Trigger / Transform / Type ID / Cause-of-transmission / Status / Sent-counts are **server-side device-driver concepts absent from the client node model** (which carries only the source link + destination address), so — as with the debugger's frame decode — they are intentionally not surfaced; the reshell shows the source→IOA rule the client actually edits. *Remaining:* the rules **grid** + destination rail (the existing `TransmissionModel` grid already edits source→IOA via `TaskManager`), per-rule enable/disable, and test-send. | `modules/transmission/`, `modules/transmission_rules/`, `main_window/` | Matches `transmission-rules.html`; CRUD + validation; per-rule enable/disable; destination link status shown. |
| 5.3 | **Debugger / protocol trace** — ✅ *landed (opt-in, slice)*: pure `debug_status`/filter logic + `RequestTableModel` (filter/clear/pause, direction+error colouring) and a reshelled debugger view with toolbar. Unit-tested. *Remaining:* no decoded APCI/ASDU detail pane; no save-to-file. Original scope: request/response frame trace with direction/type/error coding, filters, pause/clear/save, and a decoded APCI/ASDU + raw-hex inspector; device-metrics themed (`debugger.html`). 🔶 *in progress (opt-in)*. **Scope note:** the client debugger (`modules/debugger/`) traces the **client↔server session requests** (`SessionDebugger` events: title / body / response_body / phase), not device IEC-104 frames — the mockup's APCI/ASDU/IOA decode is server-side device data the client never sees, so the reshell surfaces the request/response trace the client actually has. **Reshelled trace toolbar landed**: under the opt-in theme `Debugger::CreateRequestView` wraps the trace + detail splitter in a themed toolbar — **Pause/Resume** (`RequestTableModel::SetPaused`), **Clear** (`::Clear`), a **filter** field (`::SetFilter`, matched by the pure `DebugRequestMatches` — title or request-id substring, case-insensitive) and **Save trace** (dumps the visible rows via `QFileDialog`); rows are **status-coloured** in `GetCell` from the pure `DebugStatusFor` (Failed→bad, Running→uncertain) through the severity single source (empty under legacy). The model gained a filtered `visible_` view (wholesale `NotifyModelChanged` refresh, fine for the bounded `kMaxRequests` trace). Legacy keeps the bare splitter. Pure logic unit-tested (`DebugStatusTest` + `DebugRequestMatchesTest`, 4 cases); Russian labels in `client_ru.ts`. *No headless capture:* the trace is live session data with no fixture, and `SessionService` is a broad pure-abstract interface (costly to fake) — validated by the pure tests + build/link. *Remaining:* the decoded-frame inspector (needs a server-side frame source), the direction/format filter chips, and `device_metrics` theming. | `modules/debugger/`, `modules/device_metrics/` | Matches `debugger.html`; live capture with pause/clear/filter/save; frame decode inspector; maps frame → NodeId. |
| 5.4 | **Bulk create / delete wizard** — ✅ *landed (opt-in, slice)*: Qt-free `bulk_create_pattern.{h,cpp}` (`{n}` token expansion, addressing/naming patterns, collision detection) + `qt/bulk_create_preview_panel` review step. Unit-tested; capture `bulk-create.png`. *Remaining:* it is a **preview panel, not a multi-step wizard** — no target&type or defaults steps, and no delete path; rename to *Wizard* per the parity matrix once the steps exist. Original scope: multi-step flow (target &amp; type → naming/addressing pattern with `{n}` tokens → defaults → review) with a **live preview** grid and per-item conflict resolution (skip/rename/overwrite) (`bulk-create.html`). 🔶 *in progress (opt-in)*. **Pattern engine + live preview landed** (`modules/bulk_create/`, new `client_bulk_create` module): the wizard's Naming/Addressing step (the mockup's centre) as a cross-platform (`UI_QT`) `BulkCreatePreviewPanel` over a pure, unit-tested pattern engine. `ExpandTokens` expands `{n}` (decimal), `{nn}` (zero-padded), `{hex}` in a name/NodeId template for a running index; `ExpandBulkCreate` expands a `BulkCreateParams` (name + NodeId templates, start index / count / index step, IOA start / step) into preview rows, flagging each row whose expanded NodeId is already in the target (an existing-NodeId set), and `SummarizeBulkCreate` tallies the "N new / M conflict" counts. The panel edits those fields and shows a **live preview** grid (# / Name / NodeId / IOA / Status) that re-expands on every edit, colouring conflicts (red *exists*) via the severity single source, plus the summary line. Opt-in via `MakeBulkCreatePreviewPanel` (gated on `GetSeverityTheme() != kLegacy`). Russian labels in `client_ru.ts`. Unit-tested (`ExpandTokensTest` + `ExpandBulkCreateTest` token/expansion/conflict/step/summary truth-tables + `BulkCreatePreviewPanelTest` widget: populate / conflict-flag+summary / count-spin live update / name-template re-expand — 13 cases) and validated headless as `bulk-create.png` (demo pattern TS{n} current × 12 from IOA 4001, TS8 seeded as a conflict → red *exists*). **Scope note:** the actual node creation is **not** re-implemented here — it rides the existing `MultiCreateModel::Run` (`Session.addNodes` via `TaskManager`); the multi-step wizard navigation (target&type / defaults / review), per-item conflict resolution (skip/rename/overwrite) and delete flow, plus wiring the panel into the create command's dialog, remain. | `modules/multi_create/`, `modules/bulk_create/`, `Session.addNodes` | Matches `bulk-create.html`; preview updates live from the pattern; conflicts flagged with resolution; create/delete verified by re-browse. |
| 5.5 | **Vocabulary parity check** with `web` component names. ✅ *landed*: the parity matrix is [`docs/ux/vocabulary-parity.md`](vocabulary-parity.md) — a client-owned mapping of desktop reshell classes ⇄ web design-system components (`web/ds-bundle/components/**`), covering the shell regions (ActivityBar/Explorer/Inspector/StatusStrip/ContextBar/CommandPalette) and every engineering/operator surface, with a **Status** column (Aligned / Drift / Desktop-only / Web-only) and an **Action items** list for the naming drift to reconcile (e.g. `TransmissionRuleInspector`→*Editor*, `BulkCreatePreviewPanel`→part of a *Wizard*, `Debugger` vs *ProtocolDebugger*, `FileSystemView` vs *FileBrowser*). No cross-repo file dependency — shared vocabulary, not shared files; linked from `shell.md` §4.4 and the UX README. Maintained on every reshell rename/new surface. | docs | `ActivityBar`/`InspectorPanel`/`StatusStrip`/… names align 1:1 with the web parity matrix (no shared files). |
| 5.6 | **Screenshot regen** for all reshelled views. ✅ *landed (validation)*: every reshelled view has a headless capture regenerated under `--theme=dark` into `client/docs/screenshots/` — the standalone panels (`device-diagnostics`, `series-inspector`, `users-rbac`, `transmission-rule`, `bulk-create`) plus the theme-gated views (`config-parameters`, `config-address-map`, `config-limits`, `hardware-tree`, `object-tree`) and `substation-display`. **Manifest tags updated & consistent**: the theme-gated view captures gained entries under a new **`reshell-theme`** tag (kept out of the no-theme managed pipeline — the subtab `click_object` needs the theme; the trees need it for status dots); every reshell capture is `published: false` (validation-only, not in scada-docs `img/`); `series-inspector`'s stale `referenced_from` was corrected. The manifest validator now passes **0 errors** (was 6). A `client_screenshot_check_themed` ctest renders all `reshell-theme` captures under `--theme` so the reshell chrome has a CI regression net. Regen workflow + the two capture kinds documented in `docs/screenshots.md`. **Publishing** the reviewed subset into the manual (scada-docs `img/`) remains a Windows-pipeline step (macOS is validation-only), gated per-image on visual review before it enters `current_generator_owned_subset`. **First manual adoption of reshell captures (2026-07-20):** the user manual gained a dedicated page — scada-docs `client/workbench.md` («Экспериментальный интерфейс», + `en/` mirror and `i18n_pages.yml` row) — describing the opt-in (`Ux/Experimental` / `SCADA_UX_EXPERIMENTAL`, `Ux/Theme`, `Ux/StyleSheet`), the Table toolbar, the mono-numeral/Quality-column rendering and the Inspector; it embeds two new published captures: `table-workspace.png` (a second Table spec, `reshell-theme`, in the themed-ctest `--only` list) and `inspector-panel.png` (standalone Inspector chrome, `auto-view`, expression-row fill — dark tokens regardless of `--theme`). The Table page (RU+EN) links the toolbar section and documents the new command greying. Both PNGs are macOS renders flagged in their manifest notes for a Windows re-render; the manifest validator passes 0 errors. Rule recorded in `client/CLAUDE.md`: new user-visible functionality ships a screenshot **and** a manual description in the same effort. **Shell overview published (2026-07-20):** `CaptureMainWindow` under `--theme` saves the reshelled window as **`workbench-window.png`** (own name — the hand-maintained legacy `client-window.png` is never overwritten): activity rail with alarm badge, context bar with lit KPI tiles, explorer with filter+quality dots, journal tab with the full alarm chrome, right-dock panels, status strip. Published together with `command-palette.png` as the manual workbench page's hero + palette sections (RU+EN shell walkthrough); in the themed-ctest `--only` list. `object-tree.png` stays validation-only (its capture shows just the collapsed root; the hero covers the live explorer). | `client/tools/screenshot_generator/` | `client/docs/screenshots/` reflects the new UI; manifest tags updated. |

---

## Cross-cutting acceptance (every item)

- Coherent in **light, dark, and high-contrast** themes (principle §5).
- **Keyboard-navigable**; visible focus states.
- **Colour never the only signal** — severity/quality always carry a label or
  shape too.
- Localised via `Translate()` (English in source, Russian in `.ts`).
- A **regression unit test** accompanies behavioural changes (per
  `client/CLAUDE.md`), and the relevant `client/docs/screenshots/` image is
  regenerated in the same change.

## Sizing note

P0–P2 is the bulk of the perceived redesign and the natural first milestone
(the visible operator workbench). P3 is independently shippable and high-impact
per effort (it removes the jarring native dialogs). P4–P5 deepen and complete
coverage. None of it requires touching the data-service or profile-persistence
layers (see [`shell.md`](shell.md) §5).
