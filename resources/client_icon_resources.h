#pragma once

// Resource ids used by app/client_icon.rc. The Windows shell picks the ICON
// resource with the lowest id as the executable's icon, so the client keeps a
// single, low id here.
#define IDI_APP_ICON 1
